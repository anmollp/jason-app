{
  "name": "@anmollp/jason-mcp",
  "version": "0.1.0",
  "description": "Local MCP server for Jason's deterministic JSON tools.",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "bin": {
    "jason-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc --noEmit && node scripts/build.mjs",
    "test": "pnpm run build && node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "ajv": "^8.20.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.2",
    "tsx": "^4.20.0",
    "typescript": "^5.7.3"
  }
}
